function eval_exploration()

    eval_prioritization( ...
        base_folder = fullfile(FileNameConstructor.all_results(), "exploration"), ...
        computation_mode = "sequential", ...
        scenarios = "commonroad", ...
        optimizers = "CppSampled", ...
        should_create_snapshots = 1, ...
        Hp = 8 ...
    );

end
